###
# select latest news by groupItem uniqueName
###

source: db
params:
  action: list
  model: document
  select:
    - fields
  where:
    section: stiri
    status: published
    groupItems.uniqueName: '@{uniqueName}'
  order:
    publishedAt: DESC
  limit: '@{limit|10}'
  offset: '@{offset|0}'