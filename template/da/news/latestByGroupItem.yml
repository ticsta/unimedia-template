###
# select latest news by groupItem
###

source: db
params:
  api: list
  model: document
  select:
    - fields
  where:
    section: stiri
    status: published
    groupItems.uniqueName: '@{groupItem}'
  order:
    publishedAt: DESC
  limit: '@{limit}'