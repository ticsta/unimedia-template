###
# select latest news by category slug
###

source: db
params:
  action: list
  model: document
  select:
    - fields
  where:
    section: news
    status: published
    groupItems.uniqueName: 'categories:@{category}'
  order:
    publishedAt: DESC
  limit: '@{limit|10}'
  offset: '@{offset|0}'