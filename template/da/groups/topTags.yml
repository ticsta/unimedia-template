###
# select top tags
###

source: db
params:
  api: list
  model: topgroupitem
  select:
    - fields
  where:
    groupName: tags
    selector: '@{selector|.}'
  order:
    count: DESC
  limit: '@{limit|10}'