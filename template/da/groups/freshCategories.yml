###
# select fresh categories
###

source: db
params:
  api: list
  model: topgroupitem
  select:
    - fields
  where:
    groupName: categories
    selector: '@{selector|.}'
    updatedAt:
      $gte: '@{time|$1h}'
  order:
    updatedAt: DESC
  limit: '@{limit|10}'