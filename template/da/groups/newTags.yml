###
# select new tags
###

source: db
params:
  api: list
  model: topgroupitem
  select:
    - fields
  where:
    groupName: tags
    selector: .
    createdAt:
      $gte: '@{time|$1h}'
  order:
    createdAt: DESC
  limit: '@{limit|10}'